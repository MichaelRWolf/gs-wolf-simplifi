.git/**
.npm-packages/**
node_modules/**
test/**

// Both *.js and *.ts files become *.gs (Google Script)
// *.ts is the source of truth for us
// *.js is the source of truth for Google (because it can't handddle anything more complex than ES3)

// Do _NOT_ push _transpiled_ files - *.js files in dist/ directory.
// GAS expects *.ts, which it renames to *.gs, from main directory.
dist/**

// Do _NOT_ push test files
*.spec.ts
babel.config.js
jest.config.js

// Until module/imort works... ignore src/ and test/
src/**
test/**
