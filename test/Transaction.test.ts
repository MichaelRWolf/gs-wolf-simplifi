describe('Transaction', () => {
    it('should perform some functionality', () => {
        // const instance = new Transaction();
        expect(1 + 1 == 2).toBe(true);
        // expect(instance.isSplit()).toBeTruthy();
    });
});
